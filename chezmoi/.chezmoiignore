# Files to never manage with chezmoi
# These stay in $HOME but chezmoi won't touch them

# Work-specific (never commit these patterns)
.work-*
*-work/
*-corp/

# Sensitive (CRITICAL - NEVER MANAGE THESE)
.ssh/id_*
.ssh/*_rsa*
.gnupg/
.password-store/
.private
.extra

# Generated by tools (will be recreated)
.p10k.zsh
.fzf.zsh
.zcompdump*
.tmux/plugins/

# Neovim lockfile (managed by lazy.nvim, changes on every plugin update)
.config/nvim/lazy-lock.json

# App state/cache
.cache/
.local/share/
.config/*/cache/
.npm/
.yarn/

# IDE specific
.vscode/
.cursor/
.idea/

# macOS
.Trash/
.localized
