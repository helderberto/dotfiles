# Claude Code Configuration

Custom configuration for Claude Code CLI.

## Structure

- `agents/` - Custom agent definitions
- `docs/` - Development conventions and guidelines
- `skills/` - Custom skills (slash commands)
